# gstack-copilot-jp v0.9 — スプリントランナー デザインドキュメント

## 概要

「実装して」「作って」「あとはやっておいて」の一言で、計画判定→実装→レビュー→出荷→振り返りを自動連鎖実行するフルスプリントオーケストレーター `/go`。タスク規模に応じて計画フェーズの要否を自動判定し、小さなタスクは即座に実装に入り、大きなタスクは `/autoplan` を自動挟む。

## 問題

gstack-copilot-jpは「プロセスである」と謳っているが、実行時は「ツール集」になっている。「考える→計画する」と「作る→出荷する」の間に断絶がある。計画の出力が実行の入力に自動変換されず、人間がタスクリスト・実行順序・スキップ判定を頭の中で組み立ててスキルを手動ディスパッチしている。40スキルの認知負荷より、**計画と実行が接続されていないこと**が本質的な摩擦。

## ターゲットユーザー

自分自身 — gstack-copilot-jpの作者であり、日常的に autoplan → tdd → review → ship → retro のフローを手動実行している開発者。

## 提案するアプローチ: スプリントランナー（アプローチB）

### Before / After

```
Before:
  ユーザー: /autoplan
  Copilot: [計画完了 — 推奨: /tdd]
  ユーザー: /tdd           ← 手動
  Copilot: [実装完了]
  ユーザー: /review        ← 手動
  Copilot: [レビュー完了]
  ユーザー: /ship          ← 手動
  Copilot: [出荷完了]
  ユーザー: /retro         ← 手動
  Copilot: [振り返り完了]

After（大きなタスク — 計画済み）:
  ユーザー: /autoplan
  Copilot: [計画完了 — 推奨: /go]
  ユーザー: /go
  Copilot: [tdd → review → ship → retro 自動実行]
         → 味覚判断のみ停止して質問
         → 失敗時は自動リトライ → 3回失敗でエスカレート
         → 完了レポート出力

After（大きなタスク — 計画なし）:
  ユーザー: /go ユーザー認証モジュールを追加
  Copilot: [規模判定: 大 → autoplan を自動実行]
         → [計画完了 → tdd → review → ship → retro]
         → 味覚判断のみ停止

After（小さなタスク）:
  ユーザー: /go バリデーションエラーメッセージを日本語化
  Copilot: [規模判定: 小 → 計画スキップ、直接 tdd へ]
         → [tdd → review → ship → retro]
```

## `/go` スキル設計

### フェーズ0: コンテキスト収集 & 計画判定

1. `DESIGN.md` を読み込む
2. `.gstack/plans/` にプランファイルがあれば読み込む
3. 直前の `/autoplan` または `/plan-*-review` のレビュー結果がセッション内にあれば参照
4. プロジェクトの技術スタック検出（package.json, pyproject.toml 等）
5. **タスク規模を判定**:
   - **小**（1ファイル、既存パターンの適用、バグ修正）→ 計画スキップ、直接フェーズ1へ
   - **中**（複数ファイル、新機能）→ 簡易計画（eng-reviewのみ自動実行）
   - **大**（アーキテクチャ変更、新モジュール）→ `/autoplan` を自動実行
   - ユーザーが事前に `/autoplan` 済み → 判定をスキップ
   - ユーザーが `/plan-*-review` を個別実行済み → その結果を尊重

**計画品質ゲート**: DESIGN.mdがない + autoplanレビュー未実施 + 規模が「大」の場合、警告を出す（ユーザー主権で強行可能）。

結果に基づき実行計画を生成:

```
━━━ /go スプリントランナー ━━━

プロジェクト: [名前]
計画ソース: autoplan レビューサマリー
検出したスタック: TypeScript + Vitest + React

実行計画:
  1. 作る    → /tdd（RED→GREEN→REFACTOR）
  2. レビュー → /review（サブエージェント並列dispatch）
  3. 出荷    → /ship（テスト→PR作成）
  4. 振り返り → /retro（セッションサマリー）

スキップ候補:
  - /design-html: UIスコープなし → スキップ
  - /benchmark: パフォーマンス要件なし → スキップ
  - /cso: セキュリティ重要スコープなし → スキップ

続行しますか？ (Y/n)
```

### フェーズ1: 作る（Build）

**メインスキル**: `/tdd`

```
for each task in plan.tasks:
  1. /tdd の RED→GREEN→REFACTOR サイクルを実行
  2. テスト PASS → 次のタスク
  3. テスト FAIL → /build-fix で自動修復（最大3回）
  4. 3回失敗 → ユーザーにエスカレート
     → ユーザー介入後、続行 or 中止を選択
  5. タスク完了 → git commit
```

**条件付きサブスキル**:
- UI変更を含むタスク → `/design-html` を挟む
- 既存バグ報告がある → `/investigate` を先に実行

### フェーズ2: レビュー（Review）

**メインスキル**: `/review`

```
1. /review を実行（サブエージェント並列dispatch）
2. 自動修正可能な指摘 → 自動修正 + テスト再実行
3. 味覚判断が必要な指摘 → ユーザーに提示
4. 全指摘解消 → 次フェーズへ
5. 未解消の指摘あり → ユーザーに確認（続行 or 修正）
```

**条件付きサブスキル**:
- UI変更あり → `/design-review` も実行
- DX変更あり → `/devex-review` も実行

### フェーズ3: 出荷（Ship）

**メインスキル**: `/ship`

```
1. 全テスト実行（最終確認）
2. VERSION更新
3. git commit + push
4. PR作成
5. 失敗時 → /build-fix → リトライ
```

**注**: `/ship` の中で `/review` を再実行する設計だが、フェーズ2で実行済みなのでスキップフラグを渡す。

### フェーズ4: 振り返り（Reflect）

**メインスキル**: `/retro`

```
1. セッション内の全フェーズの結果をまとめる
2. /retro でスプリント振り返り
3. /learn で学習記録を自動抽出
4. /document-release が必要ならドキュメント更新
```

## 味覚判断ゲート

自動で進めるものと、止めて聞くものの分類:

| 判断タイプ | 自動/停止 | 例 |
|-----------|----------|-----|
| テスト追加方針 | 自動 | 「カバレッジ80%以上を維持」 |
| デザイン選択 | 停止 | 「ボタン配置A vs B」 |
| API命名 | 停止 | 「createUser vs addUser」 |
| リファクタリング範囲 | 自動 | 「変更したファイルのみ」 |
| バージョン番号 | 自動 | 「semverに従い自動判定」 |
| PR先ブランチ | 自動 | 「main（デフォルト）」 |
| 破壊的変更の有無 | 停止 | 「public APIの変更を含む場合」 |

自動判断には `/autoplan` と同じ6つの意思決定原則を適用。

## 失敗リカバリー

```
┌─────────┐    FAIL     ┌───────────┐    FAIL (3回)    ┌──────────────┐
│ タスク   │───────────▶│ /build-fix │───────────────▶│ ユーザー判断  │
│ 実行     │            │ 自動修復   │                 │ 続行/中止/skip│
└─────────┘            └───────────┘                 └──────────────┘
     │                      │
     │ PASS                 │ PASS
     ▼                      ▼
┌─────────┐            ┌─────────┐
│ 次の    │            │ テスト   │
│ フェーズ │            │ 再実行   │
└─────────┘            └─────────┘
```

ユーザー判断の選択肢:
- **続行**: 手動修正後に再開
- **中止**: スプリント全体を停止
- **スキップ**: このタスクをスキップして次へ

## 実行ログ

各フェーズ完了時に進捗を出力:

```
━━━ Phase 1/4: 作る [完了] ━━━
  タスク: 3/3 完了
  テスト: 47 passed, 0 failed
  コミット: abc1234, def5678, ghi9012
  所要時間: 12分
  リトライ: 1回（Task 2 → /build-fix で解消）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 完了レポート

```
┌────────────────────────────────────────────┐
│ /go スプリント完了                           │
├────────────────────────────────────────────┤
│ Phase 1 作る:     3/3 タスク完了 ✓          │
│ Phase 2 レビュー: 5件指摘 → 5件修正 ✓       │
│ Phase 3 出荷:     PR #42 作成 ✓             │
│ Phase 4 振り返り: 学習記録 3件追加 ✓         │
├────────────────────────────────────────────┤
│ 合計コミット: 7                              │
│ テスト: 52 passed                           │
│ 味覚判断: 2件（ユーザー回答済み）            │
│ リトライ: 1回                                │
│ 所要時間: 約25分                             │
└────────────────────────────────────────────┘
```

## 既存スキルとの関係

### 残すスキル（ユーザー直接呼び出し可能）

| スキル | 理由 |
|--------|------|
| `/office-hours` | 計画の起点。インタラクティブ性が本質 |
| `/autoplan` | 計画フェーズのオーケストレーター |
| `/plan-ceo-review` | 精度が必要なとき個別実行 |
| `/plan-eng-review` | 同上 |
| `/plan-design-review` | 同上 |
| `/plan-devex-review` | 同上 |
| `/go` | **新規** — 実行フェーズのオーケストレーター |
| `/browse` | 独立したブラウザツール |
| `/investigate` | デバッグは計画なしで発生する |

### `/go` が内部で呼ぶスキル（直接呼んでもよいが、通常は不要）

`/tdd`, `/build-fix`, `/review`, `/design-review`, `/devex-review`, `/clean`, `/ship`, `/retro`, `/document-release`, `/learn`

### 変更なし（パワーツール・ユーティリティ）

`/design-consultation`, `/design-shotgun`, `/design-html`, `/qa`, `/qa-only`, `/cso`, `/benchmark`, `/canary`, `/land-and-deploy`, `/multi-plan`, `/multi-execute`, `/second-opinion`, `/loop`, `/careful`, `/freeze`, `/guard`, `/unfreeze`, `/open-browser`, `/setup-browser-cookies`, `/pair-agent`, `/setup-deploy`, `/upgrade`

## copilot-instructions.md への影響

ルーティングテーブルに追加:

```
| 実装して、作って、あとはやっておいて | `/go` |
| （/office-hours 完了後の自然な続行） | `/go` |
```

`/autoplan` の最終レポートの推奨スキルを `/tdd` → `/go` に変更。

## 技術的考慮事項

1. **スキル間のデータ受け渡し**: Copilotのセッション内コンテキストに依存。フェーズ間で明示的にサマリーを生成して次フェーズの入力にする
2. **コンテキスト消費**: 4フェーズ連続実行はコンテキスト窓を大量消費する。各フェーズ完了時にコンパクションを実行（context-management.instructions.md 適用）
3. **既存 `/loop` との棲み分け**: `/loop` はプランファイルのタスクリストを実行。`/go` はスプリントプロセス全体をオーケストレーション。`/go` のフェーズ1内部で `/loop` のパターンを借用する

## 成功指標

- ユーザーが手動でスキルを呼ぶ回数: 5回 → 1回（`/go` のみ）
- 味覚判断以外でスプリントが停止する回数: 0回（失敗は自動リカバリー）
- 小タスクのスプリント完了までのユーザー操作: `/go 〜して` → (味覚判断に回答) → 完了
- 大タスクのスプリント完了までのユーザー操作: `/office-hours` → `/autoplan`（or `/plan-*-review`）→ `/go` → (味覚判断に回答) → 完了

## スコープ外

- スキルの削除・統合（SKILL.mdの数は変えない）
- VS Codeのスキル一覧UIの制御（Copilotの仕様に依存）
- `/autoplan` 自体の改修（最終レポートの推奨先変更のみ）
- マルチモデルルーティング（`/go` は現在のモデルで実行）

## 工数見積もり

| タスク | 人間 | AI |
|--------|------|-----|
| `/go` SKILL.md 作成 | 45分 | 20分 |
| 計画自動判定ロジック設計 | 15分 | 10分 |
| `/autoplan` 推奨先変更 | 5分 | 5分 |
| copilot-instructions.md ルーティング追加 | 5分 | 5分 |
| 実プロジェクトで検証（fire-sanbo） | 1時間 | 30分 |
| リトライ・エッジケース調整 | 1時間 | 30分 |
| **合計** | **約3.5時間** | **約2時間** |
