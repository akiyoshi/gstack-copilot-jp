#!/usr/bin/env bash
# gstack-post-tool-log — postToolUse hook
# ツール実行後の成果物記録用（将来拡張用のスタブ）
# 現在は no-op。Phase 3 以降で timeline-log 連携を追加する
set -euo pipefail

# 将来: ツール実行の結果をログに記録
# TOOL_NAME="${COPILOT_TOOL_NAME:-unknown}"
# TOOL_RESULT="${COPILOT_TOOL_RESULT:-}"

exit 0
