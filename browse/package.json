{
  "name": "gstack-copilot-jp-browse",
  "version": "0.2.0",
  "description": "Playwright ベースのブラウザ自動化サーバー＆CLI（Bun）",
  "type": "module",
  "scripts": {
    "start": "bun src/server.ts",
    "cli": "bun src/cli.ts",
    "build": "bun build --compile --outfile dist/browse src/cli.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  }
}
